
fn make_counter() {
    let count = 0;
    fn inc() {
        count = count + 1;
        return count;
    }
    return inc;
}

let c = make_counter();
println("counter 1: " + str(c()));
println("counter 2: " + str(c()));
println("counter 3: " + str(c()));
